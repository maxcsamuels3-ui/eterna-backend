export default function Home() {
  return (
    <main style={{padding:24,fontFamily:"system-ui"}}>
      <h1>Eterna Backend</h1>
      <p>API live at <code>/api/health</code> and <code>/api/generate</code>.</p>
    </main>
  );
}
